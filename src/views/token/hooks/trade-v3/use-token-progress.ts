export const useTokenProgressV3 = () => {
  const total = 0
  const current = 0
  const progress = 0
  const isLoadingProgress = false
  const isFetchingProgress = false
  const refetchProgress = () => {}

  return {
    total,
    current,
    progress,
    isLoadingProgress,
    isFetchingProgress,
    refetchProgress,
  }
}

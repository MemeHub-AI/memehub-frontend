import React from 'react'

import { Button } from '@/components/ui/button'

export const MainPage = () => {
  return (
    <main className="min-h-main flex flex-col gap-2 justify-center items-center">
      <h1>Main page</h1>
      <Button>Click</Button>
    </main>
  )
}

export default MainPage

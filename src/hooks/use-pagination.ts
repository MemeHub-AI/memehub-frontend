import { last } from 'lodash'
import { useEffect, useState } from 'react'

const perPage = 10

export const usePagination = (length = 91) => {
  const pages = Array.from({ length }, (_, i) => i + 1)

  return {}
}
